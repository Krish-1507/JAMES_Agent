name: Security

on:
  push:
    branches: [main, master]
  pull_request:
  schedule:
    - cron: "17 3 * * 1"

permissions:
  contents: read

jobs:
  sca-sast:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install project and security scanners
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install "pip-audit" "bandit==1.9.4"
      - name: Dependency vulnerability scan
        run: pip-audit
      - name: Static security analysis
        # B110/B112 (try/except pass|continue) are intentional, audited
        # exception swallows in non-security-critical paths; everything else
        # is enforced. The remainder run under the repo's nosec annotations.
        run: bandit -q -r james -x tests -s B110,B112

  codeql:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      packages: read
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: github/codeql-action/init@v3
        with:
          languages: python
      - uses: github/codeql-action/analyze@v3
