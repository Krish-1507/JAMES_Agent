# Core runtime (matches the `dependencies` in pyproject.toml — that file is
# the source of truth for installs; this file is kept for tooling that reads
# requirements.txt directly).
python-dotenv>=1.0.0
requests>=2.31.0
rich>=13.7.0
cryptography>=42.0.0
beautifulsoup4>=4.12.0
openai>=1.30.0
anthropic>=0.25.0
google-genai>=1.0.0

# Optional capabilities install via extras, e.g.:
#   pip install -e ".[ui,mcp,browser,voice,desktop,docs,memory]"
#   pip install -e ".[all]"
